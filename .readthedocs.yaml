# Read the Docs configuration file for the open-invoice-docs project.
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for all options.

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

formats:
  - htmlzip

python:
  install:
    - requirements: docs/requirements.txt
